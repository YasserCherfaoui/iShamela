# ishamela-data

Python pipeline that turns Hugging Face datasets into searchable iShamela book bundles.

This package currently ships the SPEC-001 Arabic search normalizer (`ishamela_data.normalizer`). The bundle builder is SPEC-002.
