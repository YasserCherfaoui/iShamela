/// Copy to `app/lib/firebase_local_secrets.dart` (gitignored).
/// Empty keys compile, and Firebase init falls back to guest mode.
class FirebaseLocalSecrets {
  static const String webApiKey = '';
  static const String androidApiKey = '';
  static const String iosApiKey = '';
}
